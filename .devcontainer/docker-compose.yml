version: "4.0"

services:
  dev:
    image: mcr.microsoft.com/devcontainers/typescript-node:1-20-bookworm
    volumes:
      - ../..:/workspaces:cached
    command: sleep infinity
    networks:
      - app-network 

  mongo:
    image: mongo:latest
    networks:
      - app-network

  networks:
    app-network: